# @hermes-ui/ui

Placeholder package for future shared UI primitives.

Slice 01 keeps components inside `apps/web` while the design shell settles. Shared components can move here once duplication appears.
